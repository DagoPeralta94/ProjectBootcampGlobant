<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/ivPortadaSelect"
        android:layout_width="150dp"
        android:layout_height="200dp"
        tools:src="@drawable/portada_stranger"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <RatingBar
        android:id="@+id/ratingBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        android:numStars="1"
        android:rating="5"
        app:layout_constraintTop_toBottomOf="@+id/ivPortadaSelect"/>
    <TextView
        android:id="@+id/txScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@+id/ratingBar"
        app:layout_constraintTop_toBottomOf="@+id/ivPortadaSelect"
        app:layout_constraintBottom_toTopOf="@+id/txTitleMovieSelect"
        android:layout_marginLeft="5dp"
        tools:text = "4.5"
        android:scrollbarSize="20sp"/>
    <TextView
        android:id="@+id/txTitleMovieSelect"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ratingBar"
        tools:text = "The Suicide Squad"
        android:scrollbarSize="20sp"/>
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/txTitleMovieSelect"
        tools:src="@drawable/ic_info_24"/>


</androidx.constraintlayout.widget.ConstraintLayout>